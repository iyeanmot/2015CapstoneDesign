using UnityEngine;
using System.Collections;

public class edu_camera : MonoBehaviour {
        
	// Use this for initialization
	void Start () {
        gameObject.SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
	    
	}
}
