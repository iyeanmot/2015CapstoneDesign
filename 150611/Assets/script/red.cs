using UnityEngine;
using System.Collections;

public class red : MonoBehaviour {
    public bool red_check = false;
    
	// Use this for initialization
	void Start () {
        gameObject.SetActive(false);
	}
	
	// Update is called once per frame
	void Update () {
	}
}
